# 기획서 분석 및 구현 범위

## 결론

기획서의 핵심은 자동매매 시스템이 아니라, 다음 네 가지 통제점을 갖춘 근거 기반 PAPER 리서치 시스템입니다.

```text
데이터 수집 → 근거 정규화 → Research → Critic → Python Risk → 최종 결정 → 기록
```

## 핵심 요구사항

| 영역 | 기획서 요구 | 이번 구현 |
|---|---|---|
| 운영 모드 | DEV / PAPER 우선, 실거래 제외 | `PAPER` 고정 기본값 |
| 시장 데이터 | Toss API를 MarketSnapshot으로 정규화 | `MockMarketDataProvider`로 동일 스키마 구현 |
| 근거 | SEC 우선, IR·뉴스 보조, A~E 등급 | `MockEvidenceCollector`, 근거 ID·등급·URL 저장 |
| 분석 | Research Agent가 점수·Bull/Bear·제안 결정 생성 | 구현 |
| 검증 | Critic Agent가 최소 3개 실패 시나리오 제시 | 구현 |
| 리스크 | LLM과 분리된 Python 하드 필터 | 구현, YAML 설정값 기반 |
| 최종 결정 | BUY 등 7개 상태 | `EXCLUDE / WAIT / CONDITIONAL_BUY` 포함 구현 |
| 기록 | SQLite는 정형 기록, Obsidian은 사람이 읽는 기억 | 둘 다 구현 |
| 감사 | run_id, 결과, 오류, 근거 추적 | `analysis_runs`, output tables, `system_logs` 구현 |
| UX | Discord slash commands | CLI 명령으로 먼저 검증, Discord adapter는 다음 단계 |

## MVP에서 의도적으로 뺀 것

- 자동 주문, 자동 매매, LIVE_TRADING
- 실시간 미국시장 전체 스크리닝
- 실제 Toss·SEC·LLM·Discord 인증 연결
- 다중 전문 에이전트 토론 및 ML 예측
- 과거 데이터 walk-forward 평가

이 범위는 기획서의 “먼저 한 종목의 전체 경로를 끝까지 검증한다”는 원칙에 맞춘 것입니다. 외부 연동을 먼저 넣으면 데이터·네트워크·모델 오류가 섞여 핵심 의사결정 흐름을 검증하기 어려워집니다.

## 구현된 수직 슬라이스

`python main.py analyze IONQ`를 실행하면 다음이 순서대로 일어납니다.

1. `run_id` 생성 및 SQLite 실행 로그 시작
2. MarketSnapshot과 CompanyState 생성
3. SEC/IR/뉴스 Mock EvidenceItem 수집
4. Research Agent가 7개 점수와 주장별 evidence ID 생성
5. Critic Agent가 결함 및 실패 시나리오 생성
6. Python Risk Engine이 가격·시가총액·거래대금·Reward/Risk를 검사
7. 리스크 결과가 Research/ Critic보다 우선하는 최종 결정 생성
8. SQLite에 모든 중간 결과 저장
9. Obsidian 호환 Markdown 보고서 생성

## 다음 우선순위

1. SEC 공식 API collector: CIK 조회, accession deduplication, 문서 캐시
2. Toss adapter: 인증·rate limit·장외시간·오류 상태를 MarketSnapshot으로 통합
3. LLM adapter: JSON Schema 검증, evidence ID 강제, retry 상한, 비용 로그
4. Discord adapter: slash command, 사용자 허용 목록, 결과 요약 전송
5. Paper simulation: 결정 시점 이후 1/5/20/40일 결과를 Decision Log에 기록

