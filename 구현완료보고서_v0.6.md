# Stock Agent Hybrid PAPER v0.6 구현 완료 보고서

작성일: 2026-08-09  
기준 지시서: `stock_agent_Discord_자연어명령_토론_보고서_프로토타입_추가변경지시서_v0.6.md`

## 1. 최종 결론

v0.6 지시서에 따라 기존 Hermes-first 단독 Gateway 계획을 운영 경로에서 분리하고, Discord 자연어 입력을 Python Chairman 런타임 하나가 수신하는 Hybrid PAPER 구조로 재구현했습니다. Codex는 개발·보수 도구일 뿐 런타임 구성 요소가 아니므로, 백그라운드 프로세스가 실행 중이면 Codex를 종료해도 Discord 명령을 처리합니다.

현재 Toss 실시간 시세, DeepSeek `deepseek-v4-flash`, Discord 봇 3개 및 세 채널 설정은 구성되어 있습니다. 자동 테스트 63개가 통과했습니다. SEC는 별도 API 키가 필요 없지만 현재 `.env`에 사용한 연락처가 SEC에서 403으로 거부되므로 다른 연락 가능한 이메일로 User-Agent를 한 번 교체해야 실 SEC 분석 전체 흐름이 열립니다.

## 2. 확정 아키텍처

`Discord 명령채널 → Chairman Python 수신/파싱/큐 → Toss·SEC·SQLite → Hermes Research → Hermes Critic → Python Risk/Sizing → Hermes Chairman → Python Final Guard → Discord 보고서제출`

- 입력 봇: Chairman 하나
- Research 봇: 토론의장 출력 전용
- Critic 봇: 토론의장 출력 전용
- Chairman 봇: 명령 응답 및 보고서제출 출력
- 내부 에이전트 통신: 구조화 객체/JSON
- Discord 메시지: 사용자 인터페이스와 관찰용 출력이며 내부 통신 수단이 아님
- 운영 모드: PAPER 고정, 실제 주문 없음
- 토론 상한: Research/Critic 기본 1회, 추가 근거 요구 시 1회 보강하여 최대 2라운드

## 3. 구현 범위

### 자연어 명령 계층

- ANALYZE, COMPARE, REANALYZE, PRICE, PORTFOLIO, REPORT, STATUS, CANCEL, HELP 의도 지원
- 가벼운 규칙 파서와 Hermes LLM 보조 파서 결합
- LLM은 의도·티커·기간·초점만 추출하며 매수/매도 결정을 만들 수 없음
- 잘못된 JSON 응답은 한 번 복구 요청 후 실패 처리
- 모호한 요청은 `WAITING_CLARIFICATION`으로 저장하고 사용자별 확인 세션 생성
- 자연어와 슬래시 명령이 동일한 `UserRequest` 처리 경로 사용

### Discord 안전장치

- 정확한 서버·명령채널·소유자/허용 사용자 검사
- 모든 봇 메시지 무시
- Discord 메시지 ID 데이터베이스 중복 방지
- 단일 분석 큐와 종목별 활성 중복 방지
- 명령채널, 토론의장, 보고서제출의 역할 분리
- 최종 보고서는 Markdown 첨부 파일로 발행
- Discord 발행 오류는 최대 3회 재시도하고 분석 전체를 재실행하지 않음

### 데이터·의사결정 안전장치

- Toss 실시간 시세와 SEC 공시/Company Facts를 별도 근거로 수집
- 실 API 오류 시 mock 자동 대체 금지
- SEC 공시 accession 중복 제거 및 보수적 근거 등급
- Python 위험 규칙과 포지션 크기 계산
- Final Guard가 존재하지 않는 근거 ID, 위험 규칙 위반, 거래 계획 불일치를 차단
- PAPER 포트폴리오와 성과 측정 데이터 구조 유지
- 요청·확인질문·중복메시지·단계 이벤트·보고서 발행 상태를 SQLite에 기록

## 4. 검증 결과

- Python 컴파일: 통과
- 자동 테스트: 63/63 통과
- Discord Chairman Gateway 연결: 성공 (`Chairman#1070`)
- 명령채널 연결: 성공
- Toss 실시간 시세: 성공, mock 아님
- DeepSeek/Hermes 모델 호출: 성공 (`deepseek-v4-flash`)
- Research/Critic/Chairman Discord 발행: 성공
- SEC 공식 엔드포인트: 정상 User-Agent에서는 같은 IP에서 HTTP 200 확인
- 프로젝트의 현재 SEC User-Agent: 연락처 주소가 SEC에서 HTTP 403으로 거부됨

## 5. SEC 입력 교체 방법

SEC에는 API 키를 발급받아 넣는 절차가 없습니다. `.env`의 한 줄만 아래 형식으로 바꿉니다.

```env
SEC_USER_AGENT=StockAgent/0.6 다른연락가능이메일주소
```

현재 사용한 이메일이 포함되면 User-Agent 문법을 달리해도 403이고, 같은 앱 이름에 다른 연락처를 사용하면 200임을 비교 확인했습니다. 따라서 다른 이메일을 사용해야 합니다. 변경 후 런타임을 재시작하면 됩니다.

## 6. 실행 방법

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start_stock_agent.ps1
```

종료:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\stop_stock_agent.ps1
```

## 7. 남은 사용자 확인 한 가지

SEC User-Agent의 이메일을 교체하고 `명령채널`에 `IONQ 한 달 관점으로 분석해줘`를 한 번 보내면 실제 Toss + SEC + Hermes + Discord 전체 E2E 검증을 완료할 수 있습니다. 그 전에도 가격·도움말·상태·PAPER 포트폴리오 명령은 사용할 수 있습니다.

## 8. 보존된 v0.5 구성

Hermes-first MCP 서버와 프로필은 향후 v2 실험용으로 보존했습니다. 현재 v0.6 운영에서는 `run_hermes_gateway.ps1`을 함께 실행하지 않습니다. Hybrid Chairman 런타임과 동시에 켜면 같은 Discord 명령을 중복 수신할 수 있습니다.
