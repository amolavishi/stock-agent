# 적대적 검수 재작업 완료보고서 v0.2

기준 문서: `Stock Agent PAPER MVP 1차 적대적 검수 및 재작업 지시서`

## 판정

외부 Toss/DeepSeek/Discord API 연결 전에 요구된 안전성·데이터 무결성·영속성·Provider 경계 재작업을 완료했습니다. 이 버전은 여전히 PAPER/Mock Skeleton이며 실제 투자판단에 사용할 수 없습니다.

## Commit A — Safety Fixes

- 공통 ticker validator와 `^[A-Z][A-Z0-9.-]{0,9}$` 정책 적용
- 경로 이탈 문자열 및 `..`, slash, backslash, 공백, 제어문자 거부
- fixture 외 ticker hard fail
- Vault 경로 resolve 후 root 내부 여부 재검사
- Mock Evidence source를 `MOCK_SEC`, `MOCK_IR`, `MOCK_NEWS`로 분리
- Mock 보고서 최상단에 `⚠ MOCK DATA — 실제 투자판단 금지` 표시
- Agent 명칭을 `MockResearchAgent`, `MockCriticAgent`로 변경

## Commit B — Data Integrity

- EDGAR metadata evidence grade를 `UNCLASSIFIED`로 고정
- `source_reliability`, `data_quality`, `ingested_at`, `is_mock` 추가
- `MarketSnapshot`에 `stage`, `observed_at`, `ingested_at`, `is_mock` 추가
- `min_evidence`, evidence age, market freshness, Stage 3 규칙 실제 적용
- 단일 `TradePlan`에서 entry/stop/targets/reward-risk 계산
- 보고서와 Risk Engine이 같은 TradePlan 객체 사용
- heuristic 가격임을 보고서에 명시

## Commit C — Persistence

- `start_run`, `complete_run`, `fail_run` 분리
- 최초 requested/started timestamp 보존
- 기존 SQLite에 누락 컬럼을 추가하는 경량 migration
- `company_states`, `portfolio_positions`, `transactions`, `api_usage`, `model_costs` 테이블 추가
- `ObsidianKnowledgeManager`의 상태·thesis·최신 보고서 읽기와 상태·보고서 쓰기 구현
- SQLite 연결을 항상 close하여 Windows DB lock 방지

## Commit D — Provider Interfaces

- `MarketDataProvider`, `EvidenceCollector`
- `ResearchAgentProtocol`, `CriticAgentProtocol`
- async `LLMProvider.generate_json`
- `DiscordNotifier`, `MockDiscordNotifier`
- EDGAR metadata/downloader/cache/section extractor/classifier 계층 분리

## Commit E — Tests

23개 핵심 테스트를 구성했습니다.

- unknown ticker 및 path traversal
- Vault containment
- Mock 표시
- stale/min-evidence/Stage 3
- run lifecycle 및 실패 기록
- claim/evidence 연결
- TradePlan 일관성
- EDGAR grade/dedup/unsupported ticker
- Obsidian state round-trip
- notifier 및 persistence table
- POSIX ZIP entry

## 남은 범위

- Toss 실데이터 adapter
- DeepSeek 기반 LLMResearchAgent/LLMCriticAgent
- 실제 Discord slash commands
- EDGAR 원문 classifier의 LLM 연동
- PAPER forward-performance simulation

위 기능은 본 안전성 재작업 이후 단계에서 연결합니다.
